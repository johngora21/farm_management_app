import 'package:flutter/material.dart';

class ExpertSupportPage extends StatelessWidget {
  const ExpertSupportPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Expert Support'),
      ),
      body: const Center(
        child: Text('Expert Support Page'),
      ),
    );
  }
}
